@page
@model AggiungiBranoModel
@{
    ViewData["Title"] = "Aggiungi Brani";
}

<style>
    body {
        font-family: Montserrat;
    }
</style>

<h1>@ViewData["Title"]</h1>

<form method="post">

    <div class="form-group">
        <label for="titolo">Titolo</label>
        <input type="text" class="form-control" name="titolo" required />
    </div>
    <div class="form-group">
        <label for="artista">Artista</label>
        <input type="text" class="form-control" name="artista" />
    </div>
    <div class="form-group">
        <label for="anno">Anno</label>
        <input type="text" class="form-control" name="anno" />
    </div>
    <div class="form-group">
        <label for="genere"> Genere </label>
<select class="form-select form-select-lm mb-3" aria-label="Large select example" name="Genere" >
          
            @foreach (var genere in Model.Genere)
            {
                <option value="@genere">@genere</option>
            
            }
        </select>
        @* <select class="form-select" aria-label="Default select example" asp-for="Genere"
        asp-items="@(new SelectList(Model.Genere))"></select>
        </select> *@
    </div>
    <div class="form-group">
        <label for="durata">Durata</label>
        <input type="text" class="form-control" name="durata" />
    </div>


    <div class="form-group">
        <label for="immagine">Link Immagine</label>
        <input type="text" class="form-control" name="immagine" />
    </div>

    <button type="submit" class="btn btn-dark mt-4">Aggiungi</button>

</form>